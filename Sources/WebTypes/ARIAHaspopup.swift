public enum ARIAHaspopup: String, Sendable {
	case `false` = "false"
	case `true` = "true"
	case menu = "menu"
	case listbox = "listbox"
	case tree = "tree"
	case grid = "grid"
	case dialog = "dialog"
}
