extension CSS {
  public enum WordWrap: String, Sendable {
    case normal = "normal"
    case breakWord = "break-word"
    case anywhere = "anywhere"
    case initial = "initial"
    case inherit = "inherit"
  }
}
