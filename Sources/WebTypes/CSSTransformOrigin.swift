public enum CSSTransformOrigin {
	public enum Horizontal: String, Sendable {
		case left = "left"
		case center = "center"
		case right = "right"
	}

	public enum Vertical: String, Sendable {
		case top = "top"
		case center = "center"
		case bottom = "bottom"
	}
}
