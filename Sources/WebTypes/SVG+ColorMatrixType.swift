extension SVG {
  public enum ColorMatrixType: String, Sendable {
    case matrix = "matrix"
    case saturate = "saturate"
    case hueRotate = "hueRotate"
    case luminanceToAlpha = "luminanceToAlpha"
  }
}
