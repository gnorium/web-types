extension SVG {
  public enum StrokeLinejoin: String, Sendable {
    case miter = "miter"
    case round = "round"
    case bevel = "bevel"
    case inherit = "inherit"
    case initial = "initial"
  }
}
