extension SVG {
  public enum Marker {
    public enum Orient: String, Sendable {
      case autoStartReverse = "auto-start-reverse"
      public static var auto: CSS.Keyword.Auto { .auto }
    }
  }
}
